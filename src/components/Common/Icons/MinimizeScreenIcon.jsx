import { Minimize } from 'lucide-react'

const MinimizeScreenIcon = ({ className = 'h-4 w-4' }) => (
  <Minimize className={className} strokeWidth={3} aria-hidden="true" />
)

export default MinimizeScreenIcon
