import { Maximize } from 'lucide-react'

const MaximizeScreenIcon = ({ className = 'h-4 w-4' }) => (
  <Maximize className={className} strokeWidth={3} aria-hidden="true" />
)

export default MaximizeScreenIcon
